version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12
    steps:
      - checkout
      - run: yarn
      - run: yarn build
      - run: yarn deploy
workflows:
  version: 2
  build:
    jobs:
      - build
